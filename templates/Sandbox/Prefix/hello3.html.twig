<!DOCTYPE html>
    <html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Bienvenue !</title>
    </head>
    <body>
        <h1>Bienvenue {{ prenom }}</h1>
        <p>
            Mes jeux préférés sont&nbsp;:
            {% set i = 1 %}
            <ul>
                {% for jeu in jeux %}
                    <li>{{ i }}:{{ jeu }}</li>

                    {% set i = i+1 %}
                {% else %}
                    Je ne préfère aucun jeu&nbsp;!
                {% endfor %}
            </ul>
    </body>
</html>
