<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Bienvenue !</title>
    <style>
        caption { border: black solid 1px; }
        table { border: black solid 1px; }
        td { border: black solid 1px; }
    </style>
</head>
<body>
<h1>Bienvenue {{ prenom }}</h1>
<table>
    <caption>Mes jeux préférés sont&nbsp;:</caption>
    {% set i = 1 %}
    <tr><th>N°</th><th>Jeu</th></tr>
    {% for jeu in jeux %}
        <tr>
            <td>{{ i }}</td>
            <td>{{ jeu }}</td>
        </tr>
        {% set i = i+1 %}
    {% else %}
        <td>Je ne préfère aucun jeu&nbsp;!</td>
    {% endfor %}
</table>
</body>
</html>